{
  "name": "racingsite",
  "version": "1.0.0",
  "description": "A static website for racing schedules and news.",
  "main": "index.html",
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "build-with-data": "node scripts/update-f1-schedule.js && python scripts/update-indycar-schedule.py && node scripts/update-f1-results.js && node scripts/update-indycar-results.js && node scripts/update-nascar-results.js && node scripts/update-standings.js && npx @11ty/eleventy",
    "test": "echo 'No tests yet'"
  },
  "keywords": [
    "racing",
    "f1",
    "indycar",
    "nascar"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@11ty/eleventy": "^2.0.0",
    "node-fetch": "^3.3.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0"
  }
}